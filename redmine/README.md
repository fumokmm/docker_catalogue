# ビルドと立ち上げ
```
docker compose up
docker compose up -d
docker compose up -d --build
```

### 停止
```
docker-compose down -v
```

# 起動確認
```
docker compose ps
```

# ブラウザで確認
http://localhost:3000
